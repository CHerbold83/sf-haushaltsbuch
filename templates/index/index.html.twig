{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}

{% if app.user %}
<div>
    <h4 class="text-center p-3">Hallo {{ app.user.email }}! ✅</h4>
</div>
{% endif %}
<div class="text-center mb-3 mt-5">
    <a class="btn btn-secondary m-auto w-25" href={{path('edit')}}>Hinzufügen</a>
</div>
<div>
    <div class="row">
    {% for month in months %}
        <div class="col w-100">
        <a href="{{ path('detail', {'date':month}) }}" style="text-decoration:none;">
            <div class="card m-auto mt-3">
                <div class="card-header">
                    <div class="card-title text-center">
                        {{ month }}
                    </div>
                </div>
                <div class="card-body row" style="width: 20rem">
                        <p class="w-50">Einkommen</p>
                        <p class="w-50">{{totalIncome[month]|number_format(2, '.', ',')}} €</p>
                        <hr>
                        <p class="w-50">Ausgaben</p>
                        <p class="w-50">{{totalExpenses[month]|number_format(2, '.', ',')}} €</p>
                        <hr>
                        <p class="w-50">Gesamt</p>
                        <p class="w-50">{{total[month]|number_format(2, '.', ',')}} €</p>
                </div>
            </div>
            </a>
        </div>
    {% endfor %}
    </div>
</div>
{% endblock %}